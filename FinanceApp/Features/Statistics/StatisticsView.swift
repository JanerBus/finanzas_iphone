import SwiftUI

struct StatisticsView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Análisis de Finanzas")
                    .foregroundColor(.secondary)
            }
            .navigationTitle("Estadísticas")
        }
    }
}
